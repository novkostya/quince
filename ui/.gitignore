
# Playwright run artifacts — regenerated by `make gates-ui-e2e`, never committed.
# Not ignored until 2026-07-31: a gate run left `.last-run.json` in the tree and it reached a
# commit before being caught in review-of-self. The gate that produces it is routine, so the
# artifact will recur on every box that runs it.
/test-results/
